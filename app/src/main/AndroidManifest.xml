<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sizdev.arkhireforcompany">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:sharedUserId="com.sizdev.arkhire"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArkhireCompany">
        <activity android:name=".homepage.profile.talent.CreateProjectActivity"></activity>
        <activity android:name=".homepage.webviewer.ArkhireWebViewerActivity" />
        <activity android:name=".homepage.profile.talent.TalentProfileActivity" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${mapsApiKey}" />

        <activity android:name=".homepage.profile.company.CompanyProfileActivity" />
        <activity android:name=".administration.ForgetPasswordActivity" />
        <activity android:name=".administration.RegisterActivity" />
        <activity android:name=".administration.LoginActivity" />
        <activity android:name=".homepage.HomeActivity" />
        <activity android:name=".onboarding.OnBoardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>